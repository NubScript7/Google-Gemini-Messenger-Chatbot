export declare const PREFIX = "!";
export declare const COMMANDS: {
    v: string;
    logs: {
        default: string;
        run: (output: string[], password: string, logIndex: string) => void;
    };
};
